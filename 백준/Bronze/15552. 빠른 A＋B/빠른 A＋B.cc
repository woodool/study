#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int c;
	cin >> c;
	int a, b;
	for (int i = 0; i < c; i++) {
		cin >> a >> b;
		cout << a + b << '\n';
	}
}